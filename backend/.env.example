# ============================================================
# Configuration de l'application Prospection Foncière
# Copiez ce fichier en .env et renseignez vos valeurs
# ============================================================

# ---- Application ----
ENVIRONMENT=production          # development | staging | production
DEBUG=false                     # true = active /docs et /redoc
APP_VERSION=2.0.0

# ---- Serveur ----
HOST=0.0.0.0
PORT=8000
WORKERS=4                       # Nombre de workers Gunicorn

# ---- CORS ----
CORS_ORIGINS=https://votre-domaine.com,https://www.votre-domaine.com

# ---- Sécurité JWT (OBLIGATOIRE en production) ----
# Générer avec : python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ---- Base de données PostgreSQL (recommandé en production) ----
# Ces variables sont utilisées par docker-compose pour créer la DB et construire DATABASE_URL
POSTGRES_DB=prospection
POSTGRES_USER=prospection
POSTGRES_PASSWORD=changeme_mot_de_passe_fort

# DATABASE_URL est construite automatiquement par docker-compose depuis les variables ci-dessus.
# Pour un usage local sans Docker, définir directement :
# DATABASE_URL=postgresql://prospection:changeme@localhost:5432/prospection
# SQLite reste disponible pour le développement local :
# DATABASE_URL=sqlite:////data/prospection.db

# ---- Microsoft Azure AD ----
MSAL_CLIENT_ID=
MSAL_TENANT_ID=

# ---- Open WebUI (Autobot) ----
WEBUI_SECRET_KEY=changeme_secret_webui

# ---- Anthropic Claude (optionnel — module Communication) ----
# Obtenez votre clé sur : https://console.anthropic.com/
ANTHROPIC_API_KEY=

# ---- Redis Cache (optionnel) ----
REDIS_URL=redis://redis:6379/0
CACHE_TTL=300
CACHE_ENABLED=true

# ---- APIs externes ----
API_TIMEOUT=30.0
API_MAX_RETRIES=3

# ---- Rate limiting ----
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# ---- Logging ----
LOG_LEVEL=INFO
LOG_FORMAT=json
